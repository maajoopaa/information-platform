/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AuthorizationResponse } from '../../models/authorization-response';
import { CreateUserRequest } from '../../models/create-user-request';

export interface AuthorizationRegisterPost$Params {
      body?: CreateUserRequest
}

export function authorizationRegisterPost(http: HttpClient, rootUrl: string, params?: AuthorizationRegisterPost$Params, context?: HttpContext): Observable<StrictHttpResponse<AuthorizationResponse>> {
  const rb = new RequestBuilder(rootUrl, authorizationRegisterPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AuthorizationResponse>;
    })
  );
}

authorizationRegisterPost.PATH = '/authorization/register';
